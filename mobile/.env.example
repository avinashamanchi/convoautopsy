# Sanitized public client configuration. Replace these in EAS environment
# variables; never commit a real deployment value to this example file.
EXPO_PUBLIC_AI_PROXY_URL=https://your-worker.example
EXPO_PUBLIC_REVENUECAT_IOS_API_KEY=appl_example_public_sdk_key

# Groq keys, RevenueCat secret API keys, Cloudflare tokens, HMAC secrets,
# Apple credentials, and signing material belong only in their server stores.
